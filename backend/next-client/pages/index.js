export default function BackendNextPage() {
  return (
    <main style={{ fontFamily: "Arial, sans-serif", padding: "2rem" }}>
      <h1>MedRx Backend Server</h1>
      <p>
        This Next.js page is served by the custom Express server.
        Use the React frontend app for full product UI.
      </p>
    </main>
  );
}
